function PostComposer({ user, draft, setDraft, photo, onPhotoChange, mood, moods, setMood, visibility, setVisibility, onPublish, saving, error }) {
  return <section className="composer"><div className="avatar">💜</div><textarea value={draft} onChange={(e) => setDraft(e.target.value)} maxLength="5000" placeholder={`${user.ten_hien_thi} ơi, bạn đang nghĩ gì?`} />
    {photo && <div className="composer-preview"><img src={photo.preview} alt="Ảnh xem trước" /><button type="button" onClick={() => onPhotoChange(null)} aria-label="Bỏ ảnh">✕</button></div>}
    <div className="composer-options"><label className="upload-button">▣ Ảnh <input type="file" accept="image/jpeg,image/png,image/webp,image/gif" onChange={(event) => onPhotoChange(event.target.files?.[0] || null)} /></label><label>☺ Cảm xúc <select value={mood} onChange={(event) => setMood(event.target.value)}><option value="">Không chọn</option>{moods.map(item => <option key={item.idCamXuc} value={item.idCamXuc}>{item.BieuTuong} {item.TenCamXuc}</option>)}</select></label><label>◉ <select value={visibility} onChange={(event) => setVisibility(event.target.value)}><option value="Cong_Khai">Công khai</option><option value="Ban_Be">Bạn bè</option><option value="Chi_Minh_Toi">Chỉ mình tôi</option></select></label></div>
    {error && <p className="composer-error" role="alert">{error}</p>}<div className="composer-bottom"><span>{draft.length}/5000</span><button className="post-button" onClick={onPublish} disabled={saving || (!draft.trim() && !photo)}>{saving ? 'Đang đăng...' : 'Đăng bài'}</button></div>
  </section>
}
export default PostComposer
